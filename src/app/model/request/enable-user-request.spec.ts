import { EnableUserRequest } from './enable-user-request';

describe('EnableUserRequest', () => {
  it('should create an instance', () => {
    expect(new EnableUserRequest()).toBeTruthy();
  });
});
