import { SearchUserResponse } from './search-user-response';

describe('SearchUserResponse', () => {
  it('should create an instance', () => {
    expect(new SearchUserResponse()).toBeTruthy();
  });
});
